export enum REPLACETO {
    toGameField,
    toCardsSrc,
}

export enum USERSACTIONS {
    check = "check",
    continue = "continue",
}

export enum ACTIONWITHCLASS {
    add = "add",
    remove = "remove",
}

export enum STATUSSENTENCE {
    known = "known",
    unknown = "unknown",
}
